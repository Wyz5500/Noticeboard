/** Exercises forbidden access to another Feature's root-only composition entry. */
import { persistenceEntries } from '../../feature-b/public/composition/persistence.js';

export const featureAEntries = persistenceEntries;
