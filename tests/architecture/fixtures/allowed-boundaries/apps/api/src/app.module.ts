/** Represents a direct API Composition Root collecting a Feature registration entry. */
import { persistenceEntries } from './feature-b/public/composition/persistence.js';

export const applicationEntries = persistenceEntries;
