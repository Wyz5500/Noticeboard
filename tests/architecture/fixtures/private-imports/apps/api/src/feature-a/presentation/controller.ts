/** Exercises a same-layer import into another Feature's private presentation code. */
import { privateController } from '../../feature-b/presentation/controller.js';

export const featureAController = privateController;
